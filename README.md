# Email Ticket Classification & Auto-Response Bot (RPA + NLP)

This project reads email messages, classifies them into categories
(e.g., Login Issue, Payment Issue, Order Issue, General Query),
and logs the results into a structured CSV/Excel file. Later,
it can be extended to send automatic replies based on category.
